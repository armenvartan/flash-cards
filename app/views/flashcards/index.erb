<% @flashcards.each do |flashcard| %>
  <div class="created_flashcard">
    <h4>Question:</h4>
    <p><%= flashcard.question %></p>

    <h4>Answer:</h4>
    <p><%= flashcard.answer %></p>
  </div>
<% end %>
